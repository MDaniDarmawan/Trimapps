<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Trimapps" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/white</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Trimapps" parent="Base.Theme.Trimapps" />
<style name="bg_tombol">
    <item name="android:background">@drawable/background_tombol</item>
    <item name="fontFamily">@font/poppins_bold</item>
    <item name="android:textSize">20sp</item>
    <item name="android:textColor">@color/white</item>
</style>
</resources>